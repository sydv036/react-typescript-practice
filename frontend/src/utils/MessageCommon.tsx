export const MessageNotBlank = (mes: string) => {
  return `${mes} can't blank! Please enter ${mes}`;
};
export const MessageIncorect = (mes: string) => {
  return `${mes} is not corected format, please re-enter.`;
};
